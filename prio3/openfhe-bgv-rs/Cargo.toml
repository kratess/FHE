[package]
name = "openfhe-bgv-rs"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
cc = "1.0"
bindgen = { version = "0.69", optional = true }

[features]
regenerate-bindings = ["bindgen"]
